src/
├── go.mod                          # Go 模块声明
├── main.go                         # Wails v3 桌面应用入口
├── app.go                          # App 层，暴露给前端的方法绑定
├── wails.json                      # Wails 项目配置
├── .gitignore
├── README.md
├── internal/
│   ├── changedetect/detector.go    # 变动检测：文件快照对比生成变更记录
│   ├── changeorganize/organizer.go # AI 摘要：调用 LLM 提炼结构化知识
│   ├── kbindex/
│   │   ├── kbindex.go              # 知识库索引管理器（整合层）
│   │   ├── vectordb.go             # LanceDB 向量数据库封装（语义检索）
│   │   ├── graphdb.go              # Kuzu 图数据库封装（关系推理）
│   │   └── metadata.go             # SQLite 元数据主存储
│   ├── memorycore/core.go          # 记忆内核：整合 detect/organize/index
│   └── mcp/server.go               # MCP 协议服务器（与 AI 代理交互）
├── pkg/
│   ├── models/models.go            # 核心数据结构（ChangeRecord/StructuredEntry等）
│   └── utils/utils.go              # 通用工具（ID生成、目录管理等）
└── frontend/                       # React + Vite 前端
    ├── package.json, tsconfig.json, vite.config.ts
    ├── index.html
    └── src/
        ├── main.tsx, App.tsx, types.ts, styles.css
        └── components/
            ├── Dashboard.tsx       # 仪表盘：搜索/捕获/展示知识条目
            ├── KnowledgeGraph.tsx  # 知识图谱：D3.js 力导向图可视化
            └── SnapshotView.tsx    # 项目快照：版本对比与依赖差异